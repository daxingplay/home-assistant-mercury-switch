# The contents of this file is based on https://github.com/home-assistant/core/blob/dev/pyproject.toml

target-version = "py313"

[lint]
select = [
    "ALL",
]

ignore = [
    "ANN401", # Dynamically typed expressions (typing.Any) are disallowed
    "D203", # no-blank-line-before-class (incompatible with formatter)
    "D212", # multi-line-summary-first-line (incompatible with formatter)
    "COM812", # incompatible with formatter
    "ISC001", # incompatible with formatter
]

[lint.per-file-ignores]
"tests/**/*.py" = [
    "S101",    # Use of assert detected (expected in tests)
    "ANN001",  # Missing type annotation for function argument (relaxed for tests)
    "ANN201",  # Missing return type annotation for public function (relaxed for tests)
    "ARG001",  # Unused function argument (fixtures may be used via side effects)
    "PLC0415", # Import should be at top-level (sometimes needed in tests)
    "PLR2004", # Magic value used in comparison (acceptable in tests)
]

[lint.flake8-pytest-style]
fixture-parentheses = false

[lint.pyupgrade]
keep-runtime-typing = true

[lint.mccabe]
max-complexity = 25
